from chessPictures import *
from interpreter import draw
from picture import *
#draw(rock.verticalMirror())
#draw(rock.up(bishop))
#draw(rock.join(bishop.join(knight)))
draw(rock.horizontalRepeat(4))
